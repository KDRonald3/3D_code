# Horizon Map (DEPRECATED package)

> **Not the product.** See [`DEPRECATED.md`](./DEPRECATED.md).
>
> Shipping surface: [`ide/contrib/horizon/`](../../contrib/horizon/) — a first-class
> Code-OSS workbench `EditorPane`, synced into `src/vs/workbench/contrib/horizon/`.

This folder was an early VS Code extension-API scaffold (WebviewView + Extension
Development Host). Media was migrated into the workbench contrib. Do not treat
`./ide/scripts/dev-extension.sh` as the Horizon IDE product path.

```bash
./ide/scripts/bootstrap.sh
./ide/scripts/build.sh
./ide/scripts/run.sh
```
