use std::collections::HashMap;
use crate::cache::Cache;
use a::b::c::d::e::Deep;
use std::collections::{BTreeMap, HashSet};
use crate::cache::{count_hits, Cache as Alias};
use crate::outer::{self, inner::{One, Two}};
