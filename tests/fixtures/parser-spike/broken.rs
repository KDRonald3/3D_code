//! Half-typed file, as if someone is editing it right now.

use std::collections::HashMap;

/// A cache being written.
pub struct Cache {
    map: HashMap<String,
}

impl Cache {
    /// Build one.
    pub fn new(

    /// Fetch a key.
    pub fn get(&self, key: &str) -> Option<&Item> {
        self.map.get(key)
    }
}

/// Just started typing this one.
pub fn
